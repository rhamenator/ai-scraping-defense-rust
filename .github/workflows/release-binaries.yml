name: release-binaries

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.platform }} binaries
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            platform: linux-x64
            suffix: ""
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            platform: windows-x64
            suffix: .exe
          - os: macos-15-intel
            target: x86_64-apple-darwin
            platform: macos-x64
            suffix: ""
          - os: macos-15
            target: aarch64-apple-darwin
            platform: macos-arm64
            suffix: ""
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - uses: dtolnay/rust-toolchain@2eae45db285e407f22119950686d47e1101e071b # 1.88
        with:
          targets: ${{ matrix.target }}

      - name: Build release binaries
        run: cargo build --release --workspace --locked --target ${{ matrix.target }}

      - name: Package service binaries
        shell: bash
        run: |
          python scripts/package_release.py \
            --version "${GITHUB_REF_NAME:-manual}" \
            --target "${{ matrix.target }}" \
            --platform "${{ matrix.platform }}" \
            --suffix "${{ matrix.suffix }}"

      - name: Smoke test packaged service
        shell: bash
        run: |
          python scripts/smoke_release.py \
            --artifacts artifacts \
            --binary "captcha-service${{ matrix.suffix }}" \
            --port-env CAPTCHA_SERVICE_PORT \
            --port 18765

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ai-scraping-defense-rust-${{ matrix.platform }}
          path: |
            artifacts/*.zip
            artifacts/*.tar.gz
            artifacts/*.sha256
          if-no-files-found: error

  publish:
    name: Publish native binaries
    if: github.ref_type == 'tag'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          path: artifacts
          pattern: ai-scraping-defense-rust-*
          merge-multiple: true

      - name: Publish release assets
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          files: |
            artifacts/*.zip
            artifacts/*.tar.gz
            artifacts/*.sha256
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, '-') }}

      - name: Summarize binaries
        shell: bash
        run: |
          {
            echo "Published native release bundles:"
            for artifact in artifacts/*; do
              echo "- \`$(basename "$artifact")\`"
            done
          } >> "$GITHUB_STEP_SUMMARY"
